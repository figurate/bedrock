sonar:
  environment:
    - "SONARQUBE_JDBC_USERNAME=<db_user>"
    - "SONARQUBE_JDBC_PASSWORD=<db_password>"
    - "SONARQUBE_JDBC_URL=jdbc:postgresql://db_host:5432/sonardb"
haproxy:
  environment:
    - "RSYSLOG_DESTINATION=syslog_host:51501"
    - "DEFAULT_SSL_CERT="
